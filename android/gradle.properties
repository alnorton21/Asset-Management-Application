# Keep Gradle memory modest
org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=512m -Dfile.encoding=UTF-8
org.gradle.workers.max=2
org.gradle.parallel=false

# Kotlin compiler: smaller heap + avoid Parallel GC bitmap allocation
kotlin.daemon.jvm.options=-Xmx1024m,-XX:-UseParallelGC
kotlin.daemon.jvmargs=-Xmx1024m -XX:-UseParallelGC
kotlin.compiler.execution.strategy=in-process

# Use Android Studio’s bundled JDK (what your terminal shows)
org.gradle.java.home=C:\\Program Files\\Android\\Android Studio\\jbr

android.useAndroidX=true
android.enableJetifier=true
